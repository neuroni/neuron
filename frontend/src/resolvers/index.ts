// import merge from "lodash.merge";

// const merged = merge(accesslist);

// export const resolvers = merged.resolvers;
// export const defaults = merged.defaults;

export const resolvers = {};
export const defaults = {};
